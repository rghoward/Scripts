"""Day 1, Lesson 4: Functions (50 minutes)."""

# === Why this matters ===
# Functions give a reusable name to a business calculation.

# === Learning objectives ===
# Define and call functions, use parameters and return values, and recognize local variables.

# === Define and call a function ===
# A function is defined with `def`. Parameters are inputs; `return` sends a
# result back to the line that called the function.
# TODO: Define `calculate_merchandise_subtotal(unit_price, quantity)` and return the result.
# TODO: Call it for two RidgeLine Daypacks at $55.00 each and print the subtotal.

# === Parameters and return values ===
# TODO: Define `calculate_order_total(unit_price, quantity, discount_rate, shipping_cost)`.
# It should return the final total. Test it with a $189 tent, quantity 1,
# 10% discount, and free shipping. Expected total: $170.10.

# === Guided exercise: estimate margin ===
# TODO: Define `estimate_margin(unit_price, standard_cost, quantity)`.
# Test it with boots at $119.00, cost $68.00, quantity 2.
# Expected estimated margin: $102.00.

# === Independent challenge ===
# Create `shipping_charge(order_subtotal)`. Return $0.00 at $75+ and $8.99
# otherwise. Test it for $64.00 and $120.00 orders.
# Expected charges: $8.99 and $0.00.
# TODO: Write the function and labeled output.

# === Recap and preview ===
# The final Day 1 practice combines all four core ideas in one script.
