# Prompt to give Codex

Read `CODEX_BUILD_SPEC.md` and `COMPANY_AND_DATA_GUIDE.md` completely before making any changes.

Create a new directory named `python-analytics-bootcamp` and work only inside it.

The prior course archive and prior scikit-learn notebook are historical references only. Do not reuse, copy, paraphrase, refactor, or adapt their code, explanations, examples, exercises, comments, datasets, or structure. Build all new course content from scratch.

Follow the build specification exactly. Use the Blue Ridge Outfitters theme consistently, the real Open-Meteo API for the API lesson, and deterministic custom CSV/Excel business datasets for the main analysis.

Begin by:

1. Creating the complete repository structure and documentation files.
2. Generating and validating the deterministic CSV/Excel datasets.
3. Capturing a compatible cached Open-Meteo response for offline fallback.
4. Building all Day 1 instructor files.
5. Deriving the matching Day 1 starter files.
6. Running every Day 1 instructor file and checking starter/instructor synchronization.
7. Recording progress and validation results in `BUILD_REPORT.md`.

Then continue through Days 2–5 in order without asking for approval between lessons unless a genuine blocker prevents implementation.

Do not add unrequested topics or expand the scope. Do not build optional web-scraping or scikit-learn content until all core lessons pass validation.

When finished, provide a concise summary of what was created, what was tested, and any deviations from the specification.
